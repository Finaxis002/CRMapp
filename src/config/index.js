import { Platform } from 'react-native';

// सीधा localhost का यूज़ करें
const BASE_URL = 'http://localhost:5000';

export const API_BASE_URL = `${BASE_URL}/api/v1`;

export const APP_CONFIG = {
  appName: 'ShardaCRM',
  version: '1.0.0',
};
